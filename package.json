{
  "name": "mern-librarymanagementsystem",
  "private": true,
  "scripts": {
    "both": "concurrently \"npm run dev --prefix backend\" \"npm start --prefix frontend\"",
    "backend": "npm run dev --prefix backend",
    "frontend": "npm start --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}